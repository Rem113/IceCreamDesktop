<UserControl
			xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
			xmlns:local="clr-namespace:IceCreamDesktop.Presentation.Views.Controls"
			x:Class="IceCreamDesktop.Presentation.Views.Controls.IceCreamList"
			mc:Ignorable="d">

	<ScrollViewer 
		VerticalScrollBarVisibility="Auto" 
		Margin="10, 10, 0, 0">
		<ItemsControl ItemsSource="{Binding IceCreams, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:IceCreamList}}}">
			<ItemsControl.ItemsPanel>
				<ItemsPanelTemplate>
					<UniformGrid Columns="4" />
				</ItemsPanelTemplate>
			</ItemsControl.ItemsPanel>
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<MaterialDesign:Card 
						Margin="0, 0, 10, 10" 
						Height="200">
						<DockPanel>
							<Image 
								DockPanel.Dock="Top" 
								Source="{Binding ImageUrl}" 
								Height="150" 
								Stretch="Fill" />
							<TextBlock 
								DockPanel.Dock="Bottom" 
								Text="{Binding Name}" 
								TextAlignment="Center" 
								VerticalAlignment="Center" />
						</DockPanel>
					</MaterialDesign:Card>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
	</ScrollViewer>
</UserControl>